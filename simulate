<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Options Trade Simulator | WhaleWatch</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #001d2d;
      color: #ffffff;
      padding: 30px;
    }
    h1 {
      text-align: center;
      color: #00ffee;
    }
    form {
      max-width: 600px;
      margin: 0 auto;
      background: #002b3f;
      padding: 20px;
      border-radius: 8px;
    }
    label {
      display: block;
      margin-top: 1em;
      font-weight: bold;
    }
    select, input[type="text"] {
      width: 100%;
      padding: 10px;
      margin-top: 5px;
      border-radius: 4px;
      border: none;
      font-size: 1rem;
    }
    input[readonly] {
      background-color: #ccc;
      color: #333;
    }
    button {
      width: 100%;
      padding: 12px;
      font-size: 1.1rem;
      background-color: #00ffee;
      color: #001d2d;
      border: none;
      border-radius: 5px;
      margin-top: 1.5em;
      cursor: pointer;
    }
    #result {
      margin-top: 2em;
      background: #003550;
      padding: 15px;
      border-radius: 8px;
      text-align: center;
      font-size: 1.1rem;
    }
  </style>
</head>
<body>

  <h1>📊 Options Trade Simulator</h1>
  <form id="sim-form">
    <label for="symbol">Symbol:</label>
    <select id="symbol"></select>

    <label for="type">Type:</label>
    <select id="type">
      <option value="CALL">Call</option>
      <option value="PUT">Put</option>
    </select>

    <label for="strike">Strike Price ($):</label>
    <select id="strike"></select>

    <label for="expiration">Expiration Date:</label>
    <input type="text" id="expiration" readonly />

    <label for="premium">Premium Collected ($):</label>
    <input type="text" id="premium" readonly />

    <button type="submit">💥 Simulate Trade</button>
  </form>

  <div id="result"></div>

  <script>
    let whaleData = [];

    async function loadWhaleData() {
      const res = await fetch("whales.json");
      const json = await res.json();
      whaleData = json.whale_trades;
      populateSymbols();
    }

    function populateSymbols() {
      const symbolSet = new Set(whaleData.map(t => t.symbol));
      const symbolDropdown = document.getElementById("symbol");
      symbolDropdown.innerHTML = "";
      symbolSet.forEach(symbol => {
        const option = document.createElement("option");
        option.value = symbol;
        option.textContent = symbol;
        symbolDropdown.appendChild(option);
      });
      populateStrikes();
    }

    function populateStrikes() {
      const symbol = document.getElementById("symbol").value;
      const type = document.getElementById("type").value;
      const strikeDropdown = document.getElementById("strike");

      const relevant = whaleData.filter(t => t.symbol === symbol && t.type === type);
      const strikes = [...new Set(relevant.map(t => t.strike))].sort((a,b) => a - b);

      strikeDropdown.innerHTML = "";
      strikes.forEach(strike => {
        const option = document.createElement("option");
        option.value = strike;
        option.textContent = `$${strike}`;
        strikeDropdown.appendChild(option);
      });

      if (strikes.length > 0) {
        strikeDropdown.value = strikes[0];
        updateDetails();
      } else {
        document.getElementById("expiration").value = "";
        document.getElementById("premium").value = "";
      }
    }

    function updateDetails() {
      const symbol = document.getElementById("symbol").value;
      const type = document.getElementById("type").value;
      const strike = parseFloat(document.getElementById("strike").value);
      const match = whaleData.find(t =>
        t.symbol === symbol && t.type === type && t.strike === strike
      );
      if (match) {
        document.getElementById("expiration").value = match.expiration;
        document.getElementById("premium").value = match.premium.toFixed(2);
      }
    }

    document.getElementById("symbol").addEventListener("change", populateStrikes);
    document.getElementById("type").addEventListener("change", populateStrikes);
    document.getElementById("strike").addEventListener("change", updateDetails);

    document.getElementById("sim-form").addEventListener("submit", function(e) {
      e.preventDefault();
      const symbol = document.getElementById("symbol").value;
      const type = document.getElementById("type").value;
      const strike = document.getElementById("strike").value;
      const premium = document.getElementById("premium").value;
      const expiration = document.getElementById("expiration").value;

      document.getElementById("result").innerHTML = `
        You simulated a <strong>${type}</strong> on <strong>${symbol}</strong>.<br>
        Strike Price: <strong>$${strike}</strong><br>
        Premium Collected: <strong>$${premium}</strong><br>
        Expiration: <strong>${expiration}</strong>
      `;
    });

    loadWhaleData();
  </script>
</body>
</html>
