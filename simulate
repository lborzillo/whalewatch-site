<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Options Trade Simulator | WhaleWatch</title>
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      background-color: #001d2d;
      color: #ffffff;
      margin: 0;
      padding: 20px;
    }
    nav {
      background-color: #002b3f;
      padding: 1em;
      position: sticky;
      top: 0;
      z-index: 1000;
      text-align: center;
    }
    nav a {
      color: #00ffee;
      margin: 0 15px;
      text-decoration: none;
      font-weight: bold;
    }
    h1 {
      text-align: center;
      color: #5fffe0;
    }
    .form-group {
      margin: 15px auto;
      max-width: 500px;
    }
    label {
      display: block;
      margin-bottom: 5px;
      font-weight: bold;
    }
    select, input[type="text"], input[type="date"] {
      width: 100%;
      padding: 10px;
      font-size: 1rem;
      border: none;
      border-radius: 5px;
    }
    button {
      width: 100%;
      padding: 12px;
      font-size: 1rem;
      background-color: #5fffe0;
      color: #001d2d;
      border: none;
      border-radius: 5px;
      margin-top: 10px;
      cursor: pointer;
      font-weight: bold;
    }
    #result {
      margin: 25px auto;
      max-width: 600px;
      background: #002b3f;
      padding: 15px;
      border-radius: 5px;
      line-height: 1.6;
    }
  </style>
</head>
<body>
  <nav>
    <a href="index.html">Home</a>
    <a href="learn.html">Learn</a>
    <a href="simulate.html">Simulator</a>
  </nav>

  <h1>📊 Options Trade Simulator</h1>
  <p style="text-align:center;">Practice selling puts and calls like a pro — using real whale data.</p>

  <section id="simulator">
    <div class="form-group">
      <label for="symbol">Symbol:</label>
      <select id="symbol">
        <option value="NVDA">NVDA</option>
        <option value="AAPL">AAPL</option>
        <option value="TSLA">TSLA</option>
      </select>
    </div>

    <div class="form-group">
      <label for="type">Type:</label>
      <select id="type">
        <option value="PUT">Put</option>
        <option value="CALL">Call</option>
      </select>
    </div>

    <div class="form-group">
      <label for="strike">Strike Price ($):</label>
      <input type="text" id="strike" placeholder="e.g. 105">
    </div>

    <div class="form-group">
      <label for="expiration">Expiration Date:</label>
      <input type="date" id="expiration">
    </div>

    <div class="form-group">
      <label for="premium">Premium Collected ($):</label>
      <input type="text" id="premium" placeholder="e.g. 1.50">
    </div>

    <div class="form-group">
      <button onclick="simulateTrade()">💥 Simulate Trade</button>
      <button onclick="resetForm()">🔁 Reset</button>
    </div>

    <div id="result"></div>
  </section>

  <script>
    function simulateTrade() {
      const symbol = document.getElementById("symbol").value;
      const typeRaw = document.getElementById("type").value;
      const type = typeRaw.charAt(0) + typeRaw.slice(1).toLowerCase();
      const strike = document.getElementById("strike").value;
      const expiration = document.getElementById("expiration").value;
      const premium = document.getElementById("premium").value;

      if (!strike || !expiration || !premium) {
        document.getElementById("result").innerHTML = "<strong>Please fill out all fields.</strong>";
        return;
      }

      const formatted = `
        📝 You simulated a <strong>${type}</strong> on <strong>${symbol}</strong> at 
        <strong>$${strike}</strong>, collecting <strong>$${premium}</strong> in premium.<br>
        ⏳ It expires on <strong>${expiration}</strong>.<br><br>
        💡 If the stock stays above (Call) or below (Put) the strike — you keep the cash!
      `;
      document.getElementById("result").innerHTML = formatted;
    }

    function resetForm() {
      document.getElementById("strike").value = "";
      document.getElementById("expiration").value = "";
      document.getElementById("premium").value = "";
      document.getElementById("result").innerHTML = "";
    }
  </script>
</body>
</html>
